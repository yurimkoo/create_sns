<br><br>

<center><h3><b style="color:pink;">Sign Up</b></h3>
<b>Simple Registration</b>
<h5>간단한 가입으로 서비스를 이용하세요.</h5>
<br>
<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email, :label => 'Email', required: true, autofocus: true %>
    <%= f.input :password, required: true, hint: ("최소 #{@minimum_password_length}자 이상" if @minimum_password_length) %>
    <%= f.input :password_confirmation, :label => 'Confirm Password', required: true %>
  </div>

<br>
  <div class="form-actions">
    <button action="submit" class="btn btn-sm" style="color: pink; font-weight: bold; background-color: gray;">Sign Up</button>
  </div><br>
<% end %><br>

<%= render "devise/shared/links" %>
